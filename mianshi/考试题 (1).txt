考css, js, php/java, mysql 几个方面的知识, 时间1天, 记得导出数据库, 账号root, 密码root, java的导出war, 都要有源码
要求不用后台框架(比如tp, ssh, ssm等), 但可以使用前端样式框架, 前端脚本框架只能使用jquery, 不能使用插件

1. 界面按照这个来, 上面是个轮播图, 最下面是几个按钮, 做成弹性页面
2. 内容来自数据库(随机读取)
3. 答案要求多选, 答案保存数据库
4. 回答一题后, 自动到下一题
5. 全部回答结束, 给分, 并保存数据库

做的不一定要多好, 但要完成, 看看能力所长

